import { Text } from "@chakra-ui/react";
import React from "react";

const Footer = () => {
  return (
    <div>
      <Text className="foot">Input is automatically saved</Text>
    </div>
  );
};

export default Footer;
